% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/REPORT_pois.R
\name{REPORT_pois}
\alias{REPORT_pois}
\title{Generate a MultiMAP analysis report template (Poisson endpoint)}
\usage{
REPORT_pois(
  x,
  path = NULL,
  file = NULL,
  title = "MultiMAP analysis report",
  author = "",
  format = c("qmd", "rmd"),
  overwrite = FALSE
)
}
\arguments{
\item{x}{a Poisson MultiMAP object produced by \code{\link{SCI_pois}} or
\code{\link{PI_pois}} (single-arm or multi-arm).}

\item{path}{output directory. \code{NULL} (default) writes into
\code{tempdir()} (CRAN policy: do not write to the user's filespace unless a
location is given explicitly); pass an explicit directory (e.g.
\code{getwd()}) to write elsewhere.}

\item{file}{file name of the report within \code{path}. \code{NULL} (default)
uses \code{"MultiMAP_report.qmd"} (qmd) or \code{"MultiMAP_report.Rmd"}
(rmd). A full path may also be given, in which case its directory overrides
\code{path}.}

\item{title}{report title (YAML).}

\item{author}{report author (YAML).}

\item{format}{\code{"qmd"} (Quarto, default) or \code{"rmd"} (R Markdown).}

\item{overwrite}{overwrite an existing report file? Default \code{FALSE}.}
}
\value{
The report file path, invisibly. As a side effect it writes the report
  file and the \code{"<file>_object.rds"} it loads.
}
\description{
Writes a Quarto (.qmd) or R Markdown (.Rmd) report template for a Poisson
MultiMAP object (the output of \code{\link{SCI_pois}} or \code{\link{PI_pois}}),
suitable as a starting point for the statistical analysis plan (SAP) of a
study. The fitted object is saved next to the report as an \code{.rds} file;
the template loads it and rebuilds every table and figure with live code
chunks, so the document re-renders reproducibly. The italicised prose is
placeholder guidance for the user to replace.
}
\details{
Both object types share a block of three sections: the prior (\code{$prior})
as a table and density plot; the prediction interval (\code{$pred_int}) for
the concurrent control rate as a table and plot; and the robustified prior
(\code{$prior_rob}). \code{\link{SCI_pois}} output additionally gets an
analysis overview, the historical control data, the current trial, the
posterior distributions, the simultaneous credible intervals (table, plot and
interpretation) and a reference list. \code{\link{PI_pois}} output gets the
overview, the historical control data, the shared block and the reference list.

The prior / robustified-prior / posterior mixture densities are drawn with
RBesT's own \code{plot} method; the prediction-interval figure and the
simultaneous-credible-interval figure use the generic
\code{\link{plot.MultiMAP}} method (\code{which = 1} and \code{which = 2}).

For a multi-arm borrowing object (\code{histdat} supplied to
\code{\link{SCI_pois}} / \code{\link{PI_pois}} as a named list), the template
instead contains one \dQuote{Borrowed arms} section per borrowed arm -- each
with that arm's historical data, prior, prediction interval, robustified prior
and (for \code{SCI_pois} input) posterior -- followed by the non-borrowed
groups and the simultaneous credible intervals.
}
\examples{
\donttest{
histdat <- data.frame(events = c(10, 13, 14, 15, 16, 17, 19, 20),
                      offset = rep(2, 8))
newdat  <- data.frame(group  = c("control", "d1", "d454"),
                      events = c(10, 18, 21),
                      offset = c(3, 3, 3))
res <- SCI_pois(histdat, newdat, prior_est = "MAP",
                contr = "rate_diff", seed = 1)
REPORT_pois(res)   # writes into tempdir()
}
}
\seealso{
\code{\link{SCI_pois}}, \code{\link{PI_pois}}, \code{\link{REPORT_norm}},
  \code{\link{REPORT_binom}}
}
